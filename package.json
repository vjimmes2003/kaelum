{
  "name": "kaelum-portfolio",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages"
  },
  "dependencies": {
    "astro": "^5.16.9"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
